# config.yml

backend:
  name: github
  repo: roselleautoservices/roselleautoservices.github.io
  branch: master # Branch to update (optional; defaults to master)
  base_url: '/'
  local_backend: true
media_folder: 'assets/uploads'
# collections:
# - name: 'pages'
#   label: 'Pages'
#   editor:
#     preview: true
#   folder: '_site'
#   summary: "{{title | upper}} - {{date | date('YYYY-MM-DD')}}"
#   fields:
#     - { label: 'Title', name: 'title', widget: 'string' }
#     - { label: 'Publish Date', name: 'date', widget: 'datetime' }
collections:
  - name: pages
    label: Pages
    label_singular: 'Page'
    folder: /
    create: true
    # adding a nested object will show the collection folder structure
    nested:
      depth: 100 # max depth to show in the collection tree
      summary: '{{title}}' # optional summary for a tree node, defaults to the inferred title field
    fields:
      - label: Title
        name: title
        widget: string
      - label: Body
        name: body
        widget: markdown
      - label: "Tags"
        name: "tags"
        widget: "list"
        allow_add: false
        fields:
        - {label: Name, name: name, widget: string, default: "Bodyshop"}
        # fields: ["tags"]
        # default: ["Roselle Auto Services"]
      # - label: "Layout"
      #   name: "layout"
      #   widget: "list"
      #   add_to_top: true
      #   fields: ["home", "gallery", "page", "services"]
      #   default: ["page"]
    # adding a meta object with a path property allows editing the path of entries
    # moving an existing entry will move the entire sub tree of the entry to the new location
    meta: { path: { widget: string, label: 'Path', index_file: 'index' } }



  # files:
  #   - label: 'About Page'
  #     name: 'about'
  #     file: '_site/about/index.md'
  #     fields:
  #       - { label: 'Title', name: 'title', widget: 'hidden', default: 'about' }
  #       - { label: 'Layout', name: 'layout', widget: 'hidden', default: 'about' }
  #       - { label: 'Body', name: 'body', widget: 'markdown' }